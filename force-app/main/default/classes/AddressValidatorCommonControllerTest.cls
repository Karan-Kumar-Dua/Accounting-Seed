@IsTest
private class AddressValidatorCommonControllerTest {
    // AddressValidatorCommonController (test subject) is no longer used
    @IsTest
    private static void testPass() {
        System.assert(true);
    }
}