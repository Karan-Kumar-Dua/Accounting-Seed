@isTest
private class ScheduledRevenueExpensesFromFAssetTest {
    // ScheduledRevenueExpensesFromFixedAsset (test subject) is no longer used
    @IsTest
    private static void testPass() {
        System.assert(true);
    }
}